# VymoaGaurd PHM Graph Vault

Open this directory as an Obsidian vault. The persistent graph source is `../graph.json`; the human audit is `../GRAPH_REPORT.md`; the standalone browser explorer is `../graph.html`.

The graph was manually extracted from the complete Markdown brief because the graphify Python package was unavailable. Confidence labels are preserved in the JSON and report.
